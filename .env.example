# ============================================
# VARIÁVEIS DE AMBIENTE - WALLET API
# ============================================
# Copie este arquivo para .env e ajuste os valores
# ============================================

# BANCO DE DADOS POSTGRESQL
POSTGRES_USER=admin
POSTGRES_PASSWORD=senha_forte_aqui
POSTGRES_DB=wallet_db
POSTGRES_PORT=5432

# APLICAÇÃO SPRING BOOT
SERVER_PORT=8081
SPRING_PROFILES_ACTIVE=prod

# CONEXÃO COM BANCO (usadas pelo Spring Boot)
DB_URL=jdbc:postgresql://localhost:5432/wallet_db
DB_USER=${POSTGRES_USER}
DB_PASSWORD=${POSTGRES_PASSWORD}

# CONFIGURAÇÕES JPA/HIBERNATE
DDL_AUTO=update
SHOW_SQL=false
HIBERNATE_SQL_LOG_LEVEL=WARN
HIBERNATE_BINDER_LOG_LEVEL=WARN

# SEGURANÇA
# Em produção, use um secret forte para JWT (se implementado)
JWT_SECRET=seu_secret_jwt_aqui

# AWS CONFIG (para deploy na AWS)
# AWS_ACCESS_KEY_ID=seu_access_key
# AWS_SECRET_ACCESS_KEY=seu_secret_key
# AWS_REGION=us-east-1

# ============================================
# INSTRUÇÕES:
# 1. Copie este arquivo para .env
# 2. Ajuste os valores conforme seu ambiente
# 3. NUNCA commit o arquivo .env no git
# 4. Para Docker: exporte as variáveis ou use docker-compose --env-file
# ============================================